import { ConstraseñaPipe } from './constraseña.pipe';

describe('ConstraseñaPipe', () => {
  it('create an instance', () => {
    const pipe = new ConstraseñaPipe();
    expect(pipe).toBeTruthy();
  });
});
